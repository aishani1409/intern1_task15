# intern1_task15
1. Created multiple threads to represent different users trying to book tickets at the same time.
2. Used a shared variable to represent the total number of available tickets.
3. Showed how a race condition can occur when multiple threads access and modify the ticket count simultaneously.
4. Resolved the concurrency problem by using the synchronized keyword.
5. Ensured the ticket booking process is thread-safe so tickets are not oversold.
6. Used Thread.sleep() to simulate real-world delay during booking.
7. Explained the different stages in a thread’s lifecycle.
8. Displayed clear and well-formatted booking logs to show what each thread is doing.
